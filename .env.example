# chuk-mcp-maritime-archives environment variables
# Copy to .env and fill in your values

# ============================================================================
# Artifact Store Configuration
# ============================================================================

# Storage provider: memory (default), s3, filesystem
CHUK_ARTIFACTS_PROVIDER=memory

# S3 configuration (required when CHUK_ARTIFACTS_PROVIDER=s3)
# BUCKET_NAME=maritime-archives-sandbox
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_ENDPOINT_URL_S3=

# Filesystem configuration (required when CHUK_ARTIFACTS_PROVIDER=filesystem)
# CHUK_ARTIFACTS_PATH=/tmp/maritime-artifacts

# Session provider (optional; defaults to in-memory sessions)
# REDIS_URL=redis://localhost:6379/0

# ============================================================================
# Reference Data from Artifact Store (Optional)
# ============================================================================
# When set, the server preloads reference data from the artifact store
# instead of requiring local data/ files. Useful for multi-server deployments
# where you don't want to run download scripts on every instance.
#
# Generate with: python scripts/upload_reference_data.py
# MARITIME_REFERENCE_MANIFEST=

# ============================================================================
# Server
# ============================================================================
# Force stdio transport mode (set to any value)
# MCP_STDIO=
